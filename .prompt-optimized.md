<optimized_prompt>
<role>
Eres un desarrollador senior especializado en JavaScript/HTML para una aplicación web de análisis estadístico (StatAnalyzer Pro). Conoces en detalle la estructura del módulo ReporteManager.js y el flujo de firma electrónica 21 CFR Part 11.
</role>

<context>
La aplicación tiene dos contextos donde aparecen campos de firma:

1. **ReporteManager.js (~línea 2079-2095):** Durante la generación/reporte, hay una sección "Firmas" con campos editables (nombre, cargo, fecha) para roles Prep/Rev/App. El usuario rellena estos campos junto con otros metadatos del reporte.

2. **firmarReporte (indexx.js ~línea 515-530):** Página dedicada donde se carga un reporte .html ya generado y se firma electrónicamente con verificación de contraseña.

El problema: los campos de firma en el formulario de generación permiten "pre-firmar" el reporte antes de que esté en el flujo formal de firmas, lo que rompe el proceso regulatorio 21 CFR Part 11 donde la firma debe ocurrir SOLO en la página dedicada de firmas.
</context>

<task>
Elimina la sección de firmas del formulario de generación de reportes en ReporteManager.js, de modo que las firmas solo puedan realizarse desde la página "firmarReporte" con la verificación de contraseña correspondiente.
</task>

<instructions>
1. Localiza en `js/managers/ReporteManager.js` el bloque que genera la sección "Firmas" (alrededor de líneas 2079-2095, identificado por `<div class="rep-card">` con título `${t('ui_signatures')}`).
2. Elimina ese bloque completo, incluyendo su `<div class="rep-card">` envolvente y todo su contenido interno (los 3 roles prep/rev/app con sus inputs).
3. Ajusta los IDs de los previews de fecha si existen referencias a `rep-prep-date-preview`, `rep-rev-date-preview`, `rep-app-date-preview` en las líneas 2192-2202 — elimina las referencias a estos IDs de firma, manteniendo solo `rep-collect-preview`.
4. NO modifiques el botón "Enviar a firma" (id="rep-btn-sign") ni su event listener — ese flujo debe permanecer intacto.
5. NO modifiques la página `firmarReporte` en `indexx.js`.
</instructions>

<constraints>
- No rompas otras funcionalidades del formulario de reportes.
- No modifiques el flujo de "Enviar a firma" → `firmarReporte`.
- No toques el archivo `indexx.js` ni otros archivos.
- No añadas nuevos elementos ni estilos.
- El cambio debe ser puramente eliminatorio: quitar la sección de firmas del formulario y sus referencias de previews de fecha.
</constraints>

<output_format>
Proporcióname el código exacto a eliminar y el código modificado resultante para cada cambio, indicando:
- Archivo
- Líneas originales a eliminar
- Código tras el cambio
- Verificación de que no quedan referencias huérfanas a IDs de firma en el mismo archivo
</output_format>

<example>
Ejemplo de cambio similar (no es el que debes hacer, solo ilustra el formato):

**Archivo:** `js/managers/ReporteManager.js`
**Líneas originales (2079-2095):**
```html
<!-- Firmas -->
<div class="rep-card">
  ...
</div>
```
**Código tras cambio:** (eliminar completamente)
**Verificación:** `grep -n 'rep-prep-date\|rep-rev-date\|rep-app-date' ReporteManager.js` debe mostrar solo las líneas en el event listener.
</example>
</optimized_prompt>
